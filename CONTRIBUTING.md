# 开发规范

本文档约定 EADM 的日常开发方式，方便人类开发者和 AI 辅助工具保持一致。

## 开发流程

1. 从最新主分支创建功能分支。
2. 阅读相关模块代码和 `wiki/` 文档，确认现有实现方式。
3. 小步提交，单次改动聚焦一个功能、修复或文档主题。
4. 修改完成后运行必要验证，并在提交或 PR 描述中记录验证结果。
5. 涉及接口、数据库、部署、页面交互时，同步更新文档。

## 分支与提交

建议分支命名：

- `feature/<topic>`
- `fix/<topic>`
- `docs/<topic>`
- `refactor/<topic>`

建议提交信息使用简洁中文或 Conventional Commits：

```text
feat: 增加设备分配接口
fix: 修复财务导入金额解析
docs: 补充本地开发说明
refactor: 整理健康数据查询参数
```

## Erlang 后端规范

- 模块按职责拆分，命名沿用 `eadm_*`。
- 控制器命名沿用 `eadm_*_controller`。
- 路由集中维护在 `src/eadm_router.erl`。
- 公共能力优先复用 `eadm_utils`、`eadm_auth` 等已有模块。
- 新增接口时明确请求方法、参数校验、权限策略、成功和失败返回。
- 对外部输入保持校验，避免将未处理参数直接拼入 SQL 或命令。
- 日志使用项目已有日志体系，避免记录密码、Token、Cookie、支付密钥和完整个人敏感数据。

## 前端规范

- 模板文件放在 `src/views/`。
- 页面级 JS 放在 `priv/assets/js/`，命名与页面或业务模块对应。
- 公共 JS 逻辑优先放入已有公共工具模块。
- CSS 优先复用现有样式和 Bootstrap 组件，避免新增重复样式。
- i18n 文案放入 `priv/assets/i18n/` 中对应模块。
- 第三方资源位于 `priv/assets/vendor/`，不要直接改动 vendor 文件。

## 数据库规范

- 数据库脚本按数据库类型放在 `script/<db>/`。
- 修改表结构时，同步检查 PostgreSQL、MySQL/TiDB、Kingbase、Oracle、DB2 等目录是否需要对应更新。
- 初始化数据、存储过程、事件调度和定时任务需要写清楚用途。
- 不要在脚本中提交真实账号、密码、业务账单或设备轨迹。

## 文档规范

- 根目录文档用于快速上手和协作规范。
- 详细模块说明放在 `wiki/`。
- 接口变化更新 `wiki/API 接口参考/`。
- 数据库变化更新 `wiki/数据库设计/` 或项目目录结构中的数据库说明。
- 前端结构变化更新 `wiki/前端架构/`。
- 后端结构变化更新 `wiki/后端架构/`。

## 验证要求

默认验证：

```powershell
rebar3 compile
```

按改动类型追加：

- Erlang 应用启动：`rebar3 shell`
- 发布包：`rebar3 as prod release`
- Docker：`docker compose up --build`
- 前端页面：浏览器手工检查关键页面和控制台错误
- 数据库脚本：在目标数据库或兼容环境中验证初始化/迁移

如果因为本地环境缺失无法运行验证，需要在交付说明中写明。
