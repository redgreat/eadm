# Claude Project Instructions

请先阅读根目录的 `AGENTS.md`。该文件是本项目统一的 AI 协作入口，包含项目结构、命令、编码规范、安全边界和验证要求。

补充要求：

- 回答和提交说明优先使用中文。
- 修改代码前先确认现有模块模式，保持小步改动。
- 不要提交真实密钥、密码、Token、Cookie、支付配置、个人账单或设备轨迹。
- 修改接口、数据库、部署或页面结构时，同步更新 `wiki/` 或根目录文档。
- 验证失败或本地缺少工具时，明确说明原因。
